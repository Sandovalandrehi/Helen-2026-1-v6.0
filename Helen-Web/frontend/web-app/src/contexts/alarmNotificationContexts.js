/**
 * Barrel export for alarm notification context
 * Maintains consistency with existing context structure
 */
export { AlarmNotificationContext, AlarmNotificationProvider } from './AlarmNotificationContext';
